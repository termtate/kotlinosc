# bundle-dsl

Shows how to:

- build a bundle with `oscBundle`
- add nested messages
- add a nested bundle

Run:

```powershell
.\gradlew.bat runExample -Pexample=bundle-dsl
```

Expected output includes:

- the bundle timetag
- nested elements in insertion order
